<ParamField body="totalEthPrice" type="bigint">
  Total price in ETH.
</ParamField>

<ParamField body="totalUsdcPrice" type="bigint">
  Total price in USDC.
</ParamField>

<ParamField body="subtotalEth" type="bigint">
  Subtotal price in ETH.
</ParamField>

<ParamField body="subtotalUsdc" type="bigint">
  Subtotal price in USDC.
</ParamField>
