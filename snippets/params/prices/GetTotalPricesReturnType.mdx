<ResponseField name="totalEthPrice" type="bigint">
  Total price in ETH.
</ResponseField>

<ResponseField name="totalUsdcPrice" type="bigint">
  Total price in USDC.
</ResponseField>

<ResponseField name="subtotalEth" type="bigint">
  Subtotal price in ETH.
</ResponseField>

<ResponseField name="subtotalUsdc" type="bigint">
  Subtotal price in USDC.
</ResponseField>
