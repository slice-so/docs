<ParamField body="displayedPrice" type="string">
  Produce price the buyer has to pay.
</ParamField>

<ParamField body="displayedFullPrice" type="string">
  Produce price the buyer has to pay, excluding discounts.
</ParamField>

<ParamField body="isDiscounted" type="boolean">
  Boolean indicating if the product is discounted.
</ParamField>

<ParamField body="discountPercentage" type="number">
  Percentage of discount applied to the product.
</ParamField>
