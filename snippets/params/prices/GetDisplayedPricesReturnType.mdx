<ResponseField name="displayedPrice" type="string">
  Produce price the buyer has to pay.
</ResponseField>

<ResponseField name="displayedFullPrice" type="string">
  Produce price the buyer has to pay, excluding discounts.
</ResponseField>

<ResponseField name="isDiscounted" type="boolean">
  Boolean indicating if the product is discounted.
</ResponseField>

<ResponseField name="discountPercentage" type="number">
  Percentage of discount applied to the product.
</ResponseField>
